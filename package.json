{
  "name": "crispr-doorloop",
  "version": "1.0.0",
  "scripts": {
    "start": "vercel dev",
    "build": "echo \"No build step required for DoorLoop\" && exit 0"
  },
  "dependencies": {
    "resend": "^3.2.0"
  }
}
